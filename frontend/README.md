# ContractKill Frontend

React + Vite dashboard for the ContractKill SaaS optimization assistant.

## Development

```bash
npm install
npm run dev
```

Set `VITE_API_BASE` in `.env` if your backend runs on a different host.

## Build

```bash
npm run build
```

Deploy the `dist/` folder to Netlify or Vercel.
